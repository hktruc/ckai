import type {ProcessPlan} from '../../representation';import type {RenderMode} from '../../spatial-motion';import {colors,ProcessBadge,ProcessDebugCaption,useProcessPhase} from './common';import {SemanticEmbodiment} from './SemanticEmbodiment';
export const ConvergenceTransformationRenderer=({plan,renderMode,suppressEditorialLabels=false}:{plan:ProcessPlan;renderMode:RenderMode;suppressEditorialLabels?:boolean})=>{const {phase,phaseProgress,strength}=useProcessPhase(plan);const converging=phase>=1,resolved=phase>=3;const starts:[[number,number],[number,number],[number,number]]=[[170,520],[880,610],[230,1290]];const destinations:[[number,number],[number,number],[number,number]]=[[340,790],[740,790],[540,1080]];return <><ProcessBadge plan={plan} renderMode={renderMode}/><svg width="1080" height="1920" style={{position:'absolute',inset:0}}>{starts.map(([x,y],i)=>{const p=converging?(phase===1?phaseProgress:1):0,[dx,dy]=destinations[i]!;const nx=x+(dx-x)*p*strength,ny=y+(dy-y)*p*strength;return <g key={i}><path d={`M${x} ${y} Q${i===0?250:i===1?830:540} 900 ${dx} ${dy}`} fill="none" stroke={converging?colors.line:'transparent'} strokeWidth="8"/><SemanticEmbodiment object={plan.objects[i]!} x={nx} y={ny} scale={.64} opacity={resolved ? .26 : 1} suppressLabel={suppressEditorialLabels}/></g>})}<path d="M340 790 C390 980 455 1050 540 1080 M740 790 C690 980 625 1050 540 1080" fill="none" stroke={phase>=2?colors.amber:'transparent'} strokeWidth={12+(phase>=2?6*strength:0)} strokeDasharray="22 13"/><path d="M540 1080 V1230" stroke={resolved?colors.ink:'transparent'} strokeWidth="18"/>{resolved?<SemanticEmbodiment object={plan.objects.at(-1)!} x={540} y={1340} scale={1.3} suppressLabel={suppressEditorialLabels}/>:null}{phase>=4?<path d="M250 1590 H830" fill="none" stroke={colors.amber} strokeWidth="15"/>:null}</svg><ProcessDebugCaption plan={plan} phase={phase} renderMode={renderMode}/></>};
