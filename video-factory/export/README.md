# STEP 08 Final Export runtime

Mechanical FFmpeg transcode from exact reviewed STEP 07 preview to `CKAI_VERTICAL_MASTER_V1`, followed by ffprobe/full-decode, normalized all-frame SSIM, decoded audio consistency, source-equivalence QA and Release Manifest generation. Decoded-equivalence failure is a hard Publish blocker.

No creative render, caption regeneration, remix, uploader, scheduler, platform API or publishing. See `../../engine/final-export-engine.md`.
