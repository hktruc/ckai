# Vbee current API credentials (never commit real values)
VBEE_APP_ID=
VBEE_ACCESS_TOKEN=

# Optional overrides for the local Piper technical-preview runtime
PIPER_PYTHON=
PIPER_VAIS1000_MODEL=
PIPER_VIVOS_MODEL=
# OpenAI API
OPENAI_API_KEY=

# Optional Phase 1 overrides; checked-in runtime defaults are shown here for discovery.
CKAI_IMAGE_MODEL=gpt-image-2
CKAI_VISION_MODEL=gpt-5.6-terra
CKAI_GENERATED_ASSETS_TARGET_MIN=2
CKAI_GENERATED_ASSETS_TARGET_MAX=4
CKAI_GENERATED_ASSETS_HARD_MAX=5
CKAI_MAX_GENERATION_ATTEMPTS=3
CKAI_MAX_GENERATION_CALLS=9
# Leave blank when exact USD estimation is not reliable. Call-count guards still apply.
CKAI_MAX_IMAGE_USD_PER_VIDEO=
